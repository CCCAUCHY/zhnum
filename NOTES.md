# 开发与维护笔记

README 讲这个 locale 是什么、怎么用；这里讲**改它的时候要知道什么**。

## 代码结构

| 文件 | 职责 |
|------|------|
| `zhnum_core.py` | 纯逻辑：数字读法 `read()` 与别名闭包 `alias_closure()`。不读文件、不写盘、不看 argv |
| `gen_zhnum.py` | 读数 → 建槽 → 发射 locale 源。`TIER OUTDIR [pure\|mixed]` |
| `gen-charmap.py` | 从系统 charmap 生成补齐版（原因见下） |
| `update-unihan.py` | 从 Unihan.zip 重建三张 tsv；按内容比对判「有更新」 |
| `verify.py` | 排序语义验收，也是周构建的通过门槛 |
| `build_tiers.sh` / `pack_tiers.sh` | 六份产物的一键构建与打包 |
| `tools/` | 其余检查：`alias_spec.py` 别名完备性、`zb_check.py` 默认排序、`perf_nau.py` 性能、`lint_locale.py` 静态检查、`check_all.sh` 汇总 |

## 三条不能违反的不变量

1. **权重列方向**。glibc 的权重分配序是
   `基表符号 < 本地 reorder 新符号 < copy 链块符号`。
   汉字行必须借用基表已有符号（权重4 用 `<U0021>`、权重2 用 `<BASE>`），
   用自己的 token 会被数值槽符号压过，位置分区的列方向翻转。
2. **权重3 恒 IGNORE**。这层带任何值，都会在数值裁决之前插入一层与基表不对称
   的比较，打断位置分区。空着就是它的功能。
3. **别名闭包是唯一入口**。新增注册点一律调 `alias_closure()`，不要手写写法
   列表——漏掉任何一种写法，该串会退化成多原子，而多原子的比较在首原子就终止，
   首原子携带的写法序会压过后缀的数值。

## 容易踩的坑

- `localedef -i <名>` 在 cwd 无对应源文件时**静默回落** `/usr/share` 的装机版；
  实验编译必须给绝对路径。
- 码点不在 glibc 的 UTF-8 charmap 里时，localedef 会**静默丢弃**该行（无告警）。
  兼容汉字区段有 40 个这样的码点（U+FA6E/FA6F/U+FADA..U+FAFF），靠
  `gen-charmap.py` 补齐。动过字符相关逻辑后要复查生成行是否真的落地。
- 别名闭包内部是 set 派生结构，消费前必须 `sorted()`，否则同值槽的注册序会随
  `PYTHONHASHSEED` 抖动，两次构建产出不同文件。
- `pack_tiers.sh` 会把 `out/` 的产物打包提交。数据表比 `out/` 新时它报错退出，
  这是有意的，不要绕过——绕过就会提交一份用旧数据编的产物。
- 压缩流随压缩器版本变（与归档格式无关，zip/gz/tar.gz 都一样），换构建环境后
  第一次运行会产生一次提交，之后稳定。
- `localedef` 的耗时随元素量**超线性**增长：十几万元素是秒级，五十万元素在本机
  （3.8 GB 内存）超过十分钟、峰值 745 MB。编译慢由元素量决定，不是文件坏了。
- **有放回抽样 + 相邻比较 = 每轮必有一例误报**。有放回必然取到重复值（n=90 万
  抽自 1e10 期望 ~40 对，n=100 万抽自 1e12 也有 ~0.5 对），而相邻两条若是同一个
  值，「本条所有键 > 上一条所有键」必然不成立。抽样检测第一次真跑产出的 203 份
  证据全是这个 —— 逐份解开样本数组查过，每一例的前一条就是它自己，一份真错序
  都没有。`tools/sweep.py` 已改成 `sorted(set(...))`，样本指纹表达式同步加 `set`。
- 并行 job 提交各自的产物时，`git fetch` + `git reset --soft origin/main` 只挪
  HEAD 不动索引，提交出来的树 = **本 job 检出时**的旧树 + 新文件 —— 会把别的
  job 刚提交的东西从树上抹掉（sweep 的证据文件曾因此恒只剩 1 个）。用
  `--mixed`：索引回到 origin/main，工作区新写的未跟踪文件不受影响。
- **本地 glibc 比 runner 新，编译行为不能外推**。`localedef` 解析 `copy "x"`：
  本机（CachyOS 新版）在目录里只有已编译的 `x.UTF-8`、没有源文件时能成功；
  runner 的 Ubuntu 24.04 glibc 2.39 报 `cannot open locale definition file`。
  所以编译时**源文件必须和 zhnum 同目录**。同类坑之前也踩过（CI 报值序错序、
  本地复现不出）。凡涉及 glibc 行为的结论，以 runner 上的实测为准。
- shell 里 `wait` **不带参数会吞掉后台任务的失败**：并行的六份 localedef 全挂，
  步骤照样绿，job 报成功而抽样 0 条。要么逐个 `wait $pid`，要么事后正向断言
  产物存在（现在是两者都做）。

## 完美范围的天花板（2026-09 发现，待改进）

**定律**：完美范围 = ① 与 ② 取紧，**绝对封顶 1e12**。

- **① 全量枚举**：对每个 `u < TIER` 的单位枚举 `r×u`（`_rmax = min(9999, TIER//u)`）。
  连续覆盖能爬到的**规范顶级单位只有亿**（`canon` 用 亿 管 `[1e8, 1e16)`）→
  `9999×1e8 + 余数` = **1e12**。
- **② COEFS 基座**：系数表 r ≤ 100，它与每个单位的乘积全档共用，但**只能接在
  canon 的顶级单位上**——万（管 `[1e4,1e8)`）和亿。于是 `100×万 = 1e6`、
  `100×亿 = 1e10`。万亿/亿亿不是顶级单位，接不上（`c×1e12` 只有 c 是 1e4 的
  倍数时字符串里才恰好出现 `万亿`），只给出孤立的整数倍点。

实测（mixed，比值 30 倍以内的邻近对）：

| 档位单位 | 完美范围 | 倍率 | 覆盖来源 |
|---------|---------|------|---------|
| 10³ 千 | 1e6 | **1000×** | 全基座（与万档 zhnum 逐字节相同） |
| 10⁴ 万 | 1e6 | 100× | 全基座（没有 `u < 档位` 的单位可枚举） |
| 10⁸ 亿 | 1e10 | 100× | ① 铺满 1e8 以下，② 接 `100×亿` |
| 10¹² 万亿 | 1e12 | 1× | ① 封顶 |
| 10¹⁶ 亿亿 | 1e12 | **0.0001×** | ① 封顶，标称 1e16 不可达 |

**亿亿档没有存在价值**：完美范围比档位单位小四个数量级，出错时机不可预测；
多花的 27% 元素只买到更大的**写法**覆盖（亿亿/万亿），排序范围与万亿档完全相同。
发布物里留着它是为写法完整，**不要再宣传成「范围更大」**。

**天花板来自成本选择，不是数学**：`_rmax` 里的 9999 是「最大的四位数」。枚举成本
线性（每多枚举 1 个系数 ≈ **+11.2 元素**，万亿 mixed 实测）：提到 99999 → 封顶
1e13，+90 万元素（总 1.44M，3.3×）；提到 1e6 → 封顶 1e14，≈11.5M 元素（27×），
`localedef` 的超线性耗时让这条路不可行。**收益最高的是亿档**：32 万元素换 1e10。

### 深层结构：为什么不能靠更聪明的枚举解决

现在的模型是**有限字符串枚举 + 字典序**：每个读数注册成单原子，多原子串的比较在
首原子就终止。要让 `[1e12, 1e16)` 连续，得注册 1e8 个亿系数（≈1.1e9 元素）——
因为 `canon(3.699e12)` 是 `三万六千九百九十亿`，这个 merged 形式的语义是
`(三万 + 六千九百九十) × 亿`：**尾随单位要乘前面所有原子的和**（分配律）。
glibc 的 collation 是逐权重的位置比较，没有累加器也没有栈，表达不了这个语义。
障碍是权重模型本身，不是工程量。

两条绕行都要改动对外语义，故暂不做：

- **改读法**：允许 `三万亿六千九百九十亿` 这种每级写全单位的形式 → 现有枚举就能
  覆盖到 1e16。但中文不这么写，语料里不会出现，没有实用价值。
- **改权重模型**：需要「条件规则」（权重依赖已匹配的前缀），glibc 语法没有。

结论：**现实上限就是 1e12**。再往上只能扩 ②，而 ② 的扩展点只有顶级单位
（万、亿）且每级最多 100 倍。

### 对应的代码位置（要动就得同时动这些）

| 位置 | 作用 |
|------|------|
| `gen_zhnum.py` `_rmax = min(9999, TIER // _ev)` | ① 的天花板 |
| `gen_zhnum.py` `COEFS` | ② 的右端（r ≤ 100，第一个缺的是 101，即「101 倍」） |
| `gen_zhnum.py` `UNIT_CHAIN` | ② 能接的单位集合；只有 万/亿 是 canon 顶级单位 |
| `zhnum_core.py` `canon()` / `read()` | 决定 merged 形式，也就决定了哪些单位能当锚 |
| `tools/sweep.py` `FRONTIER` | 必须等于**实测**完美范围，不是喂给生成器的 TIER |
| README 三档表与「乘法分解与范围的边界」段 | 必须与实测一致 |

改任何一处之后，README 的表格和 `sweep.py` 的 `FRONTIER` 都要重新实测校对——
它们描述的是同一件事的三个副本。

## 改完必做

```bash
python3 gen_zhnum.py 100000000 out/x mixed    # 单份生成
./build_tiers.sh                              # 六份重建 + 编译 + 打包
./tools/check_all.sh                          # 排序语义 + 别名完备性 + 性能
python3 tools/lint_locale.py out/ tiers/      # 静态检查
```

改动 `gen_zhnum.py` 之后，**先用同一份数据比对重构前后的产物是否逐字节相同**，
再谈别的——这是判断改动是否只动了想动的东西的最快办法。
